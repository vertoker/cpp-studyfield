// main() is an entry function
// it must contains in executable programs
int main()
{
    // main() must return status code
    // status code is `int` type
    // 0 means all ok, other means error
    return 0;
    // return is operator, with rules
    // object for return must be compatible with declarated

    // exercise 1.2
    //return -1;
}