#include <iostream>

int main()
{
    char cval = 'a';
    int ival;
    float fval;
    double dval;

    // a
    if (fval); // fval -> bool

    // b
    dval = fval + ival; // i -> float, sum, float -> double

    // c
    dval + ival * cval; // cval -> int, mult, int -> double, sum
    
    return 0;
}