Aggregate class must not have any in-class initializers, with that it can be created with brackets

```cpp
struct Sales_data
{
    std::string bookNo;
    unsigned units_sold;
    double revenue;
};
```
