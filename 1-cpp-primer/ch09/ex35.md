`std::vector` has `size` and `capacity`

`capacity` represents real allocated memory for that count of elements

`size` persesents active count of elements in allocated memory, it can be `size<=capacity`
